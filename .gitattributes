# Normalize line endings to LF in the repo (keeps source clean across Windows/Mac).
* text=auto eol=lf

# Binary assets — never touch line endings.
*.png binary
*.ico binary
*.icns binary
*.jpg binary
*.bin binary
